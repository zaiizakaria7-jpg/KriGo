const express = require("express");
const router = express.Router();

// Les routes d'authentification locales (register, login) sont dans routes.js
// Ce fichier peut être utilisé pour d'autres types d'authentification si nécessaire (ex: social login)

module.exports = router;